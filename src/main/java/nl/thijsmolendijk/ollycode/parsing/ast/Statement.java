package nl.thijsmolendijk.ollycode.parsing.ast;

/**
 * Indicates that this node is a statement. Statements generally don't return values (and if they do, it is through the `return` keyword).
 * @author molenzwiebel
 */
public interface Statement extends Node {

}
